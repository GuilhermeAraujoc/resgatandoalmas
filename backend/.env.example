# Local development outside Docker (docker-compose sets these itself).
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/resgatando_almas?schema=public"
PORT=3001
APP_TIMEZONE="America/Sao_Paulo"
